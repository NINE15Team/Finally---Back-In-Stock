import { Form } from '@remix-run/react';
import { BlockStack, Button, ButtonGroup, Card, InlineStack, TextField } from '@shopify/polaris';
import React from 'react';

export default function EmailVerificationCard() {
    return (
        <Card padding="400">
        </Card>
    );
}
